# ── Деплой: Netlify (free) ─────────────────────────────────────────────
[build]
  command = "npm run build"
  publish = "dist"

# `uk` живе на корені `/` без префікса — старі/прямі заходи на `/uk/...` перенаправляємо на корінь.
[[redirects]]
  from = "/uk/*"
  to = "/:splat"
  status = 301

# TODO(етап 5): redirects для форми Netlify не потрібні — форма в статичному HTML.
